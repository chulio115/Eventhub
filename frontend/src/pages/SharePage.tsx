export function SharePage() {
  return (
    <div className="mx-auto max-w-4xl space-y-4 px-4 py-6">
      <h1 className="text-xl font-semibold text-slate-900">Geteiltes Event</h1>
      <p className="text-sm text-slate-600">
        Hier wird später die öffentliche, read-only Ansicht eines Events angezeigt, die über ein Sharing-Token
        erreichbar ist.
      </p>
    </div>
  );
}
